package com.anas.maslahati_pro.Services;

import org.springframework.stereotype.Service;

@Service
public class AIChatService {

    public String askAI(String message) {
        message = message.toLowerCase().trim();

        if(message.contains("مرحبا") || message.contains("اهلا") || message.contains("السلام عليكم")
                || message.contains("hello") || message.contains("hi")) {
            return "مرحباً! 😊 كيف يمكنني مساعدتك؟";
        }

        if(message.contains("كهربائي") || message.contains("electrician")) {
            return "بالتأكيد! يوجد العديد من الكهربائيين المتاحين. ما المنطقة التي تريد الخدمة فيها؟";
        }

        if(message.contains("سباك") || message.contains("سباكة") || message.contains("plumber")) {
            return "لدينا سباكين محترفين جاهزين لخدمتك. أين مكانك؟";
        }

        if(message.contains("غسالة") || message.contains("washing machine")) {
            return "يمكن ترتيب فني صيانة غسالات فوراً. أخبرني ما المشكلة؟";
        }

        if(message.contains("سعر") || message.contains("تكلفة") || message.contains("cost")) {
            return "الأسعار تختلف حسب نوع الخدمة والمنطقة. أخبرني ما نوع الخدمة لأعطيك تقديراً أدق.";
        }

        if(message.contains("اريد صنايعي في") || message.contains("صنايعي في")) {
            return "بالتأكيد! أخبرني فقط ما نوع الصنعة المطلوبة وسأساعدك.";
        }

        if(message.contains("اريد صنايعي في رام الله")) {
            return "نعم! يوجد العديد من الصنايعية في رام الله. ما نوع الخدمة التي تريدها؟ 😊";
        }

        if(message.contains("اعطال") || message.contains("مشكلة") || message.contains("تصليح")) {
            return "سأساعدك بكل تأكيد. ما نوع العطل أو المشكلة التي تواجهك؟";
        }

        if(message.contains("هل يوجد") || message.contains("هل عندكم") || message.contains("متوفر")) {
            return "أخبرني ما الخدمة التي تبحث عنها وسأخبرك بتوفرها فوراً.";
        }

        if(message.contains("شو بتعمل") || message.contains("ما هو موقعكم") || message.contains("شو مصلحاتي")) {
            return "مصلحاتي هو نظام يساعدك في العثور على أمهر الحرفيين في منطقتك بكل سهولة. 😊";
        }

        if(message.contains("باي") || message.contains("سلام") || message.contains("مع السلامة")) {
            return "مع السلامة! 👋 أنا هنا دائماً إذا احتجت أي شيء.";
        }

        return "لم أفهم رسالتك تماماً 😅، حاول صياغتها بطريقة أخرى أو اسألني عن خدمة معينة.";
    }
}
